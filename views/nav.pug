block content
  nav.navbar
    ul.nav
      li.float-left
        a(href="/")
          img(src="/images/logo.png")
      li.float-left
        a.home.navbar-brand(href='/') Home
    - if (user_id == undefined && !user) {
    div.nav
      span.float-right
        a#login(href="/")='Login'
      span.float-right
        a#register(href="/register")='Register'
    - } else if (user == undefined && !user_id) {
    div.nav
      span.float-right
        a#login(href="/")='Login'
      span.float-right
        a#register(href="/register")='Register'
    - } else {
    div.nav
      span.float-right
        a#logout(href="/logout")= 'Logout'
      span.float-right
        a#profile(href="/profile")='Account'
    - }
